import { styled } from 'styled-components';

export const Svg = styled.svg`
  background-color: ${({ theme }) => theme.white};

border-radius: 50%;

line-height: 0;
height: 1.875rem;
width: 1.875rem;
`
